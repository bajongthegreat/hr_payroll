<?php

class Work_assignmentsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('work_assignments')->truncate();

		$work_assignments = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('work_assignments')->insert($work_assignments);
	}

}
